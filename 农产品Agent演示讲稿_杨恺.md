# 农产品电商 Agent · 现场演示讲稿（5 分钟控场版）

> 用途：面试现场演示环节（或"讲讲你最近在做什么"）。
> 定位：Kender 的**能力延伸应用**——同一套 ReAct + 工具调用底座，换个业务场景长出来的产品。
> 原则：先讲场景价值，再演示，最后主动交代边界。

---

## 一、总时间线（5 分钟，按需压缩到 3 分钟）

| 时间 | 环节 | 动作 | 目的 |
| --- | --- | --- | --- |
| 0:00–0:30 | 开场定调 | 一句话讲场景 | 让面试官 30 秒理解在做什么 |
| 0:30–1:00 | 架构 30 秒 | 口述技术栈 | 亮出 Agent 底座 |
| 1:00–2:00 | 演示①建店 | 输入"我家有50斤西红柿…" | 核心卖点：自然语言→店铺 |
| 2:00–2:45 | 演示②增量更新 | 输入"加个黄瓜，10块一斤，共100斤" | 证明是"对话式经营"，不是一次性生成 |
| 2:45–3:30 | 演示③ReAct 轨迹 | 展开左侧推理轨迹面板 | 讲清 Agent 工作机制，最加分 |
| 3:30–4:15 | 讲一个真实踩坑 | position:fixed 渲染 bug | 证明排查根因的工程能力 |
| 4:15–5:00 | 边界 + 收尾 | 主动说清未做的东西 | 诚实，防追问翻车 |

> **网络/环境兜底**：提前打开 `data/shop_react_preview_demo.html` 备用；服务起不来时切静态文件讲同样的动线。

---

## 二、逐环节话术

### 1. 开场定调（30 秒）

"这是我最近做的一个应用：**农产品电商 Agent**。场景是农民不会用电商后台，只要用大白话说一句'我家有 50 斤西红柿，3 块钱一斤，帮我开个店'，Agent 就自动把店铺页面生成出来——商品卡片、价格、库存、购物车、模拟支付全都有。它把'开店'这个门槛从学后台操作降到会说一句话。"

### 2. 架构 30 秒（口述即可，不用翻代码）

"底座和我的主项目 Kender 是同一套：AgentScope 的 ReActAgent + 通义千问 qwen-plus。区别是业务工具层——这里注册了两个工具：`build_farm_shop` 建店、`update_farm_shop` 增量改店。模型自己决定调哪个、怎么填参数，我没有任何关键词硬编码。渲染层是 Gradio 6，左侧对话 + ReAct 轨迹，右侧实时渲染店铺页面。"

### 3. 演示①建店（1 分钟）

**输入**：`我家有50斤西红柿，3块钱一斤，帮我开个店`

**边等边讲**："注意模型在做三件事：理解商品名、价格、数量 → 从自然语言里抽结构化数据 → 调用 build_farm_shop 生成店铺。"

**出来后指右侧**："店名、头图、西红柿卡片、单价、库存、加入购物车按钮，全部从那一句大白话来的。"

### 4. 演示②增量更新（45 秒）

**输入**：`加个黄瓜，10块一斤，一共100斤`

**讲**："第二句来了之后，Agent 判断这是**更新**而不是重新建店，调的是 update_farm_shop——所以西红柿还在，黄瓜加进来了。这就是'对话式经营'：改价、上新、下架，全靠说话。"

### 5. 演示③ReAct 轨迹（45 秒，最加分）

展开左侧"ReAct 推理轨迹"面板，指着说：

"这是 Agent 的完整工作过程：**思考**——分析用户意图；**行动**——调用 update_farm_shop，参数在这儿；**观察**——工具返回店铺更新结果。整个循环模型自主驱动，我只注册了工具和约束了参数 schema。这也是它和'LLM 套壳'的本质区别：**决策权在模型，控制权在工具契约**。"

### 6. 踩坑故事（45 秒，证明工程能力）

"讲一个真实 bug：店铺页面在预览面板里一直空白。我先怀疑是 style 标签问题，改内联样式没用；又怀疑 iframe 加载，换 srcdoc、base64、静态文件路由三种方案都没用。最后我用 Playwright 无头浏览器对拍截图，才定位到根因：**Gradio 6 的 gr.HTML 里只要出现 position:fixed 元素，文档流主体就不渲染**。解法是渲染函数加一个 `fixed_position` 参数，预览版走文档流定位，完整交互版走独立 HTML 打开。"

（如果面试官时间紧，这个故事可以砍，优先保轨迹演示。）

### 7. 边界与收尾（45 秒）

"主动说清楚边界，省得猜：
- 预览面板是**渲染展示**，加购物车、支付的完整交互在导出的独立 HTML 里；
- 微信支付是**模拟**，不接真实支付；
- 目前是**单用户全局状态**，多会话隔离是下一步；
- 下一步计划是把店铺状态做持久化，加上对话式改装修风格。"

收尾一句："这个项目对我最大的价值是验证了一件事：**同一套 Agent 底座，换个工具层就能长出完全不同的业务**——这也是我认为 Agent 应用开发的核心能力。"

---

## 三、高频追问预判

**Q1：和直接写个表单让农民填有什么区别？**
"表单要求用户理解字段概念。这里的本质区别是 Agent 做了**意图理解 + 填槽**：'50斤西红柿3块一斤'这种语序混乱的话也能抽对结构。而且后续所有经营操作都是对话驱动，不用学任何界面。"

**Q2：模型抽错参数怎么办？**
"两个防线：一是工具参数有 schema 约束，数量、价格类型不对会拒绝；二是缺关键信息时模型会追问，不瞎猜——这个填槽策略在我的主项目里是专门调过的，去掉参数默认值 + system prompt 强制追问。"

**Q3：为什么不用多 Agent？**
"当前场景单 ReAct 循环就够了，两个工具、链路短，多 Agent 反而增加延迟和 token 成本。但架构上留了口子：店铺生成质量可以用主项目里的 Critic 模式加后置审核，这是我做过的。"

**Q4：并发怎么办？**
"现在是全局单店铺状态，演示够用。生产化路径明确：状态从全局变量挪到 session store，渲染服务化成 FastAPI 接口——这两个我的主项目里都有现成实现，属于迁移不是从零做。"

**Q5：这个项目的店铺页面是怎么生成的？**
"模板 + 数据驱动：工具返回结构化 shop_data，渲染函数把数据灌进内联样式模板。不是让 LLM 直接吐 HTML——那样不可控也不安全。LLM 负责**理解**，模板负责**呈现**，职责分离。"

---

## 四、演示前检查清单（面试当天）

- [ ] `run_demo.bat` 双击能起，浏览器开 `http://127.0.0.1:7862`
- [ ] 提前跑一遍建店 + 加黄瓜，确认 API key 有效、预览正常
- [ ] `data/shop_react_preview_demo.html` 双击可开（断网兜底）
- [ ] 演示完记得清空对话重新开始，给面试官干净的画面
- [ ] 笔记本提前连好网/开热点，DashScope 依赖网络
