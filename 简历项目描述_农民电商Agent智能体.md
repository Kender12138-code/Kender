# 简历项目描述 · 农民电商 Agent智能体（复制即用）

> 写法原则：每条都是"做了什么 + 解决了什么问题 + 技术体现"，不写"学习了/掌握了"。

---

## 版本 A：完整版（简历空间充足时用，4 条）

**农民电商 Agent智能体 —— 个人项目（基于 Kender Agent 框架）** ｜ 2026.08–至今 ｜ Python / AgentScope / qwen-plus / Gradio 6 / Playwright

- 面向不会用电商后台的农民用户，构建自然语言驱动的开店 Agent：用户一句"我家有 50 斤西红柿，3 块钱一斤，帮我开个店"，Agent 自主调用建店工具生成完整电商店铺页面（商品卡片、价格、库存、购物车、模拟支付），将开店门槛从"学后台操作"降为"说一句话"。
- 基于 AgentScope ReActAgent 实现自主工具调用与增量更新：模型自主决策调用 build_farm_shop / update_farm_shop（加商品、改价格、换风格），对话式经营而非一次性生成；工具参数缺失时主动追问而非猜测默认值。
- 独立定位并修复 Gradio 6 渲染层 bug：gr.HTML 中 position:fixed 元素导致文档流主体不渲染，通过 Playwright 无头浏览器对拍截图定位根因，以双渲染模式（预览版文档流定位 / 完整版独立页面）方案解决，全程零回归。
- 沉淀可复现的自动化验收链路：Playwright 脚本驱动"建店→上新→加购→结算→支付"全流程 E2E 录制与像素级验证，75 秒演示视频作为简历附件。项目已开源：github.com/Kender12138-code/Kender

---

## 版本 B：精简版（简历空间紧张时用，2 条）

**农民电商 Agent智能体 —— 个人项目** ｜ Python / AgentScope / qwen-plus / Gradio

- 构建自然语言开店的电商 Agent：农民一句话描述商品与价格，Agent 自主调用工具生成店铺页面（商品展示/购物车/模拟支付），并支持对话式增量上新、改价，工具调用由 LLM 自主决策、无关键词硬编码。
- 独立排查修复 Gradio 6 渲染 bug（position:fixed 导致文档流不渲染），并以 Playwright 建立全流程自动化验收（建店→加购→结算），75 秒演示视频随简历投递。开源：github.com/Kender12138-code/Kender

---

## 与 Kender 主项目的关系（简历排序建议）

```
项目经历
├── Kender —— AI 生活助手（主项目：ReAct 工具调用 / RAG / 长期记忆 / MCP / FastAPI 服务化）
└── 农民电商 Agent智能体（场景落地：同一 Agent 底座 + 业务工具层的垂直应用）
```

面试话术衔接："Kender 是我把 Agent 全链路做了一遍，农民电商 Agent 是验证**同一套底座换个工具层就能长出新业务**——这是我理解的 Agent 应用开发的核心能力。"

---

## 每条 bullet 的追问预判（面试防翻车）

| 简历表述 | 面试官可能追问 | 你的回答要点 |
| --- | --- | --- |
| "自主调用工具，无关键词硬编码" | 怎么保证不是关键词匹配？ | 工具选择由 LLM 在 ReAct 循环里决策，我只注册工具 + schema 约束参数；展开轨迹面板能看到"思考→行动→观察" |
| "参数缺失时主动追问" | 怎么做到的？ | 去掉工具参数默认值 + system prompt 强制规则，模型没有默认值可猜，只能追问（Kender 里调过的填槽策略迁移过来） |
| "定位并修复渲染 bug" | 怎么定位的？ | 三个错误假设（style 标签/iframe 加载/文件路由）逐一排除，最后用 Playwright 对拍截图二分定位到 position:fixed；体现"不绕过、找根因" |
| "对话式经营而非一次性生成" | 和每次重新生成有什么区别？ | 建店/改店是两个独立工具，Agent 识别用户意图选择调用；改店保留已有商品状态，只做增量修改 |
| "模拟支付" | 为什么不接真支付？ | 需要微信支付商户资质；演示重点是 Agent 链路而非支付通道，简历里如实写"模拟"不夸大 |

---

## 使用提醒

- 投递 AI Agent 岗 → 用版本 A，突出 ReAct 和工具调用
- 投递偏工程/后端岗 → 版本 B 第 2 条 bug 排查经历反而更打动人，可以提到第 1 条位置
- 视频附件（75 秒 MP4）比文字更能打：邮件正文引用 GitHub 链接 + 附件视频双保险
