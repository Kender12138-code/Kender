# Kender 项目面试演讲稿（杨恺 · 10 分钟版）

> 用途：校招面试中"讲讲你的项目"环节。
> 目标：8-10 分钟讲清楚 Kender 是什么、为什么值得看、技术难点怎么解决。
> 原则：诚实、具体、有边界意识——不夸大、不遮掩。

---

## 一、开场白（30 秒）

"面试官您好，我做了一个叫 **Kender** 的个人项目，是一个面向日常生活的 AI Agent。
它不是简单的聊天机器人，而是具备**工具调用、长期记忆、多 Agent 协作、文档 RAG 检索**的完整系统。
项目开源在 GitHub 上，最近我还把它服务化了，准备部署到腾讯云，作为简历上可以直接点开的作品。"

---

## 二、项目定位与核心能力（1.5 分钟）

"Kender 的定位是'你的专属 AI 生活助手'。基于 AgentScope 的 ReActAgent 和 DashScope 的 qwen-plus 搭建，目前支持三种入口：Gradio 网页、命令行、FastAPI HTTP 服务。

它有几个核心能力：

第一，**真 ReAct 工具调用**。联网搜索、天气查询、文档检索都注册为 Agent 工具，模型自己决定什么时候调用哪个，不是关键词硬匹配。

第二，**跨会话长期记忆**。每轮对话后我会抽取用户名字和关键事实，比如学校、目标、喜好，存到本地 JSON，下次启动时回灌到 system prompt。

第三，**真 RAG 文档检索**。用户上传 TXT、DOCX、PDF 后，先做中文友好的分块，再用 DashScope text-embedding-v3 向量化，存到 FAISS，检索时还会做 query 改写提升召回。

第四，**多 Agent 协作**。生成回答后，会有一道 Critic 检查点审核——答非所问、编造工具返回内容，就会被回炉重写。

第五，**MCP 协议接入**。提醒能力由独立进程的 MCP Server 提供，和主程序解耦，接入失败还会自动降级。"

---

## 三、技术亮点与真实踩坑（3-4 分钟）

### 亮点 1：Function Call 的填槽（Slot Filling）

"我遇到的第一个真实问题是：用户问'今天天气怎么样'，模型会自己猜一个城市，比如直接查北京。这不是我想要的行为。

我的解法分两步：

一是**代码层去掉参数默认值**。比如 `get_weather(city: str)`，不给默认值，模型就没法偷懒。
二是**system prompt 里写死规则**：调用工具前先检查必需参数，缺失时必须追问用户，禁止猜测。

实测下来，现在问天气会先反问'你想查哪个城市'，提醒场景也会先追问'提醒内容是什么、什么时候提醒'。"

### 亮点 2：Critic 质量审核（多 Agent 协作）

"第二个问题是模型会编造工具返回内容。比如提醒列表里明明没有日期，它却自己在回答里加一个日期。

我加了一个 Critic Agent 做后置检查点。它拿到'用户问题 + 工具实际返回 + 模型回答'三样东西做对照，判断有没有编造或答非所问。

这里有两个关键设计：

一是 Critic 输出用 Pydantic 强制结构化，输出 `{'passed': bool, 'reason': str}`，防止它絮絮叨叨多烧 token。
二是**没调用工具时直接跳过审核**，因为闲聊和追问没必要审核，省 token。

实测它确实抓到过两次编造行为，被打回重写后修正了。"

### 亮点 3：MCP 协议接入

"第三个是想让工具层更解耦。我写了独立的 MCP Server 提供提醒能力，主程序通过 MCP 协议调用。

这里踩了几个坑：

- Web 服务里不能用 stdio 传输，因为连接在 lifespan 阶段、调用在请求处理阶段，是两个 asyncio task，anyio 会报跨 task 退出 cancel scope 的错误。所以我改成 streamable-http。
- 子进程 stdout 不能接 PIPE 不读，否则日志写满缓冲区 MCP Server 会卡死。我改成写到 `data/mcp_server.log`。
- MCP 接入失败时自动降级为进程内工具，保证主流程不受影响。"

### 亮点 4：Query 改写

"为了让 RAG 检索召回率更高，我在检索前做 query 改写，把问题改写成 3 条同义语句，分别检索后合并去重。

但我做了一次对照实验：在小语料，比如只有 4 个片段时，改写没有任何增益，还多花一次 LLM 调用。

所以我加了阈值：**只有索引片段数 ≥ 8 时才启用 query 改写**。这样既保证大语料下的召回，又不浪费小语料下的成本。"

---

## 四、诚实边界（1 分钟）

"这个项目也有一些我清楚的边界，面试里我不会夸大：

第一，**流式输出是前端 incremental rendering**，不是模型 token 级 streaming。拿到完整回复后逐字 reveal，体感像流式。

第二，**Critic 和 Generator 复用同一个 qwen-plus 模型实例**，只是 prompt 角色不同，不是独立部署的第二个模型服务。

第三，**MCP Server 目前是本地子进程**，不是远程服务。

第四，**RAG 还没有做混合检索和 rerank**，只是纯向量检索。

这些都是我后续计划要做的。"

---

## 五、后续规划（1 分钟）

"接下来我会做三件事：

第一，把项目部署到腾讯云，拿到一个长期公网链接，直接挂到简历上。

第二，实现上下级式多 Agent 协作，比如 Planner 负责拆任务、多个 Worker 分别执行检索/生成/审核。

第三，把 RAG 升级到混合检索 + rerank，让检索更准确。

另外真 token streaming 也是方向，但需要改 AgentScope 调用层，复杂度更高。"

---

## 六、结束语（30 秒）

"这就是 Kender。它不是一个完美项目，但每个模块我都清楚为什么这么做、问题在哪、下一步怎么迭代。
对我个人来说，这个项目让我真正理解了 Agent 的完整链路，而不仅仅是大模型的 prompt 工程。"

---

## 七、高频问答预判（Q&A）

### Q1：为什么用 AgentScope，不用 LangChain/LangGraph？

"仓库里其实有两个 demo（kender_rag_demo / langgraph_rag_demo）做横向对比。
主项目用 AgentScope，是因为它的 ReActAgent 和 Toolkit 对面试项目更友好，代码量可控，我能把 ReAct 循环、工具注册、长期记忆每个链路讲清楚。LangGraph 的状态图能力强，但链路复杂后，面试时反而容易讲糊。"

### Q2：长期记忆和 RAG 有什么区别？

"长期记忆是显式结构化事实，比如你的名字、学校、目标，按条回灌 system prompt，记的是'关于你的事'。
RAG 是把文档向量化，按问题检索片段，查的是'文档里的内容'。一个是持久化记忆，一个是文档检索。"

### Q3：多 Agent 协作现在是主流吗？

"主流路径我把它分成四层：单 Agent → 评审式（我现在） → 上下级式（Planner+Worker） → 竞争式。
我已经做了评审式，下一步做上下级式。竞争式不太适合我现在的场景，因为会多烧 token。"

### Q4：部署后怎么防止 API Key 被刷？

"Gradio 的 `launch()` 支持 `auth` 参数，我会给网页加访问口令，只有拿到口令的人才能用。
另外 API Key 放在服务端环境变量里，不暴露给前端。"

### Q5：你最大的技术收获是什么？

"学会了'不要把 bug 藏起来'。之前记忆抽取失败时，代码里写了 `except Exception: return None, []`，
结果问题被吞了很久。后来我改成 except 里 print 日志，问题立刻暴露。
这个习惯对排查 Agent 链路特别重要，因为 Agent 的 bug 往往不是报错，而是行为看起来正常但结果不对。"

---

## 八、演示建议

如果面试环境允许，现场演示 3 句话：

1. **填槽**：输入"今天天气怎么样"，看 Kender 追问城市。
2. **RAG**：上传简历 PDF，问"我的项目经历写了什么"。
3. **轨迹可视化**：展开"推理轨迹与质量审核"面板，指着"行动 → 观察"讲 ReAct。

---

## 九、得物面试针对性补充区（待填）

> 把得物面试官具体问的问题写到这里，我会针对性补一段回答。
> 例如：
> - 得物问了**电商搜索/推荐相关** → 强调 RAG 检索、query 改写、向量召回。
> - 得物问了**多模态/图片处理** → 说明 Kender 当前还没做多模态，但 RAG 链路可以扩展图片 Embedding。
> - 得物问了**并发/服务稳定性** → 讲 lifespan 初始化、MCP 降级、健康检查 /health。
> - 得物问了**实习项目 70 迈** → 把 Kender 的 Agent 思维迁移到测试数据分析场景。
